apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    http://influxdb:8086
    secureJsonData:
      token: hydromesh_token
    jsonData:
      version: Flux
      organization: hydromesh_org
      defaultBucket: hydromesh_db
      tlsSkipVerify: true
